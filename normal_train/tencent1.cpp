#include <vector>
#include <iostream>

using namespace std;

int main(){
    int n=5;
    vector<int> nums(n,0);
    for (int i=0;i<n;i++){
        cin >> nums[i];
    }

    vector<int> heads(n,0);
    vector<bool> fail(n,True);

    
}